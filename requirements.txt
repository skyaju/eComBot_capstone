# eComBot dependencies (Day 07 - Production Containerization)
# Leave versions unpinned so pip can solve a compatible set across ADK/RAG stack.

google-adk
litellm
python-dotenv
pydantic
pydantic-settings
pytest
chainlit
asyncpg

# Redis session storage
redis

# ChromaDB vector store (RAG)
# Keep client/server versions aligned with docker-compose image chromadb/chroma:0.5.23.
chromadb==0.5.23
# fastembed: ONNX-based embeddings; no PyTorch required; ~35 MB model
fastembed

# PostgreSQL (analytics, audit, customer profiles)
SQLAlchemy
alembic
psycopg2-binary
